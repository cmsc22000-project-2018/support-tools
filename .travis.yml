language: c

compiler: gcc
    
install: true

stage: 
  - build
  - test

branches:
  only:
    - master

jobs:
  include:
    - stage: build
      script: make -C organize_dir/ all
    - stage: test
      script: echo test
