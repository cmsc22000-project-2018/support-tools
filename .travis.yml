language: c

compiler: gcc
    
install: true

stage: 
  - build
  - test

branches:
  only:
    - master

jobs:
  include:
    - stage: build
      script: make -C shell-draft-1/ all
    - stage: test
      script: echo testd
    - stage: test
      script: echo test
