language: c

compiler: gcc
    
install: true

stage: 
  - build
  - test

branches:
  only:
    - master

jobs:
  include:
    - stage: build
      script: make all
    - stage: test
      script: echo test
